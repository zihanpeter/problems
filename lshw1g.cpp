#include <iostream>
using namespace std;
const int N = 1e5 + 5;
int n, l, r, a[N];
int main() {
    scanf("%d%d%d", &n, &l, &r);
    for (int i = 1; i <= n; ++i) scanf("%d", a + i);
    
    return 0;
}